# Pangdun CRM Handoff

更新日期：2026-08-12

## 1. 产品定位

Pangdun CRM 是给硬件品牌内部团队使用的 KOL / 媒体合作管理系统。它不是传统销售 CRM，而是围绕以下主流程设计：

`媒体发现与建档 → 联系与洽谈 → 建立项目/合作 → 寄样与物流 → 内容审核/发布 → 费用结算 → 归档复盘`

当前运行形态是 Windows 局域网 MVP：FastAPI + SQLite + React/Vite。网页端负责日常操作，`pangdun.cmd` CLI 供管理员和 Agent 批量核验、清洗及审计数据。

核心设计原则：

- 胖墩品牌资产保留，整体采用温暖、轻盈、近似 Apple 原生毛玻璃的视觉语言。
- 信息密度优先，避免传统后台的大卡片、厚按钮和多层弹窗。
- 高频动作留在列表和工作台，完整资料进入统一详情抽屉。
- 同一业务事实只保存一份：项目、合作、寄样、物流、OA/PI、媒体主页等页面引用同一数据源。
- Agent/CLI 可以协助清洗，但不确定的数据进入待核验队列，不允许静默猜测。

## 2. 当前信息架构

左侧导航：

- 合作执行
- 项目管理
- 历史归档
- 媒体 / KOL
- 产品库
- 费用表导入
- 用户管理

原来的“执行工作台”和“合作执行单”已经合并为一个“合作执行”入口。页面保留工作台的视觉风格，在固定控制栏下切换三种内容视图：

- 今日待办：今天、逾期跟进、未来 7 天、全部待办。
- 全部合作：完整合作列表，包含阶段、下一步、负责人、OA/PI 和物流。
- 阶段看板：按执行阶段展示紧凑卡片。

三个视图共用新建入口、状态筛选、搜索、刷新和合作详情。切换视图时控制栏位置固定，仅替换下方内容。

## 3. 本阶段已完成

### 合作执行

- 合并执行工作台与合作执行单，删除重复导航入口。
- 完整执行状态统一为：待确认、待发货、运输中、已签收待产出、内容审核中、已发布、已结算、已暂停/取消。
- 新增轻量“流程轨道”状态栏；状态栏和顶部下拉筛选双向同步。
- 搜索跨视图保留，支持创作者、项目、OA/PI、物流单号等字段。
- 今日待办的对象名称、全部合作列表和看板卡片都进入同一个合作详情。
- 今日待办首次进入会按以下顺序自动落到第一个非空分类：今天 → 逾期跟进 → 未来 7 天 → 全部待办。
- 自动落点只执行一次；用户手动选择空分类后，跨视图返回仍保留选择。
- 修正逾期标签计数，使用真正的逾期跟进任务数，不再误用内容逾期数。

### 物流与常用单据

- OA/PI 和物流单号可在合作详情中直接查看、编辑。
- 全部合作列表显示 OA/PI 和物流单号，并支持一键复制。
- 合作详情与项目“登记寄样”共用寄样数据，不建立第二份副本。
- 后端合作列表预加载寄样数据，减少列表重复请求。

### 媒体 / KOL 数据

- 多平台主页拆分为独立的 `profile_links`，不再把多个 URL 拼进一个输入框。
- 媒体体量直接保存为 K：达人使用粉丝量，独立网站使用月访问量。
- 列表显示总粉丝/流量，不在平台标签后重复显示每个平台数字。
- 可按体量范围筛选。
- 渠道和合作状态已建立标准枚举与归一逻辑。
- 提供待核验中心，处理缺主页、缺联系人、状态无法归一等档案。
- 修复媒体质量报告参数错误、重复联系人、Beacons/Media Kit 被误识别为媒体名称等历史数据问题。
- CLI 支持媒体归一、主页拆分、合作更新和审计日志；写操作要求 `--apply` 与修改原因。

### 交互与视觉

- 移除浏览器原生下拉体验，统一使用项目内 Select/Popover 基础组件。
- 列表、抽屉、确认框、状态标签和按钮均调整为温暖、轻量、低边框风格。
- 待核验中心改为横向动作区，避免按钮文字纵向溢出。
- 编辑媒体表单修复字段高度、对齐及保存无响应问题。
- 控制栏、搜索、筛选和刷新在合作执行三个视图中保持固定位置。

## 4. 关键数据与模型决策

- `Campaign` 是单个媒体/KOL 的合作执行实例；项目可以包含多个 Campaign。
- `Project` 管理活动目标、预算和整体复盘，不取代 Campaign 的执行状态。
- 寄样记录属于 Campaign，并可关联 Project；OA/PI、承运商、物流单号和物流状态保存在寄样记录中。
- 媒体主渠道用于列表概览，真实主页以 `profile_links[]` 为准。
- `followers_or_traffic` 的存储单位统一为 K；`audience_metric_type` 区分 followers 与 monthly_visits。
- 合作执行状态与媒体合作关系状态是两个维度：前者描述某次合作流程，后者描述长期关系。
- 不可靠或无法唯一归属的数据必须进入“待核验”，不能由自动清洗强行确认。

## 5. 参考 CRM 与吸收的理念

### trycompai/crm（Agentic CRM）

仓库：<https://github.com/trycompai/crm>

主要价值是 Agent 驱动的数据整理、自动补全和面向行动的工作方式。Pangdun 借鉴其“Agent 辅助维护 CRM”的方向，但保留人工核验、变更预览、`--apply` 和审计日志，避免 Agent 不透明地覆盖业务数据。

### Attio

- 灵活的数据对象与可配置视图。
- 同一数据集可以切换列表、流程/看板等观察方式。
- 吸收结果：合作执行使用同一数据源提供今日、全部、阶段三种视图，而不是复制三套模块。

### HubSpot

- 记录详情、活动时间线、任务和 Pipeline 分工清晰。
- 高频信息可以在列表查看，完整历史进入记录详情。
- 吸收结果：合作动态统一留痕；列表突出下一步，详情承载寄样、费用、内容和活动。

### Pipedrive

- 强调“下一步活动”，没有下一步的机会应被视为需要处理。
- Pipeline 阶段适合快速判断阻塞位置。
- 吸收结果：今日待办、跟进日期、下一步行动和阶段看板成为合作执行的主要入口。

### folk

- 以联系人/关系为中心，操作轻、列表密度高。
- 吸收结果：媒体档案、联系人、主页和合作历史集中在媒体 360 视图，避免页面层级过深。

### Clay

- 数据补全、归一和工作流自动化能力强。
- 吸收结果：CLI/Agent 承担可审计的数据清洗与补全；不确定项进入人工待核验。

这些产品是结构和交互原则参考，不要求 Pangdun 复制其视觉组件。Pangdun 的视觉基线仍以当前执行工作台的温暖毛玻璃风格为准。

## 6. 下一阶段目标

优先级 P0：

1. 完善合作详情的信息架构：将寄样、内容、费用、动态整理为清晰分区或轻量页签，减少长抽屉滚动。
2. 将“下一步行动”变成强约束工作流：保存执行状态时检查下一步和日期，对缺失项给出非阻塞提醒。
3. 为阶段看板补充真正的推进交互（阶段变更、阻塞提示），但避免高风险的误拖拽。
4. ~~补齐合作执行的集成测试：智能待办落点、状态筛选同步、OA/PI/物流保存、归档恢复。~~ 已完成基础覆盖，后续随功能持续扩展。

优先级 P1：

1. 项目页升级为 Campaign 级总览：预算/实付、寄样进度、内容进度和负责人负载。
2. 完善批量操作：负责人、状态、跟进日期、优先级，并提供明确的变更预览。
3. 建立持续数据质量中心：重复媒体/联系人、失效主页、缺体量、过期核验数据。
4. 为独立网站流量和平台粉丝数据记录来源、核验日期和历史变化，而不仅保存最后一个数字。
5. 更新 README 中仍使用“执行工作台/合作执行单”旧称的操作说明。

优先级 P2：

1. 从局域网 SQLite MVP 迁移到内网服务 + PostgreSQL。
2. 增加自动备份验证、权限细化和操作审计检索。
3. 评估定时 Agent：定期发现失效主页、待跟进合作和需要重新核验的体量数据。

## 7. 已知技术债与注意事项

- `frontend/src/main.tsx` 体积过大，包含多数页面和业务逻辑；下一阶段应按 feature 拆分，但拆分前先补关键交互测试。
- `LegacyExecution` 和旧 `Workbench` 相关结构仍有可清理空间；当前 Workbench 作为合作执行的子视图继续使用，不应直接删除。
- 智能待办落点目前在前端完成；首次加载依赖 `/api/workbench` 返回的 KPI。
- 阶段看板目前是点击卡片进入详情，不支持拖拽更新状态。
- SQLite 适合当前小团队低并发使用，批量清洗前仍应先备份。
- 数据库、Token、日志和本地运行文件不应提交到 Git。
- 不在文档中记录密码。默认账号提示仅保留在 README，正式使用必须修改默认密码。

## 8. 启动、验证与常用命令

启动：

```bat
start.bat
```

本机地址：<http://127.0.0.1:8000>

前端构建：

```powershell
cd frontend
npm run build
```

CLI：

```bat
pangdun.cmd auth status
pangdun.cmd tasks today
pangdun.cmd tasks overdue
pangdun.cmd collab list
pangdun.cmd media normalize
pangdun.cmd audit --limit 20
```

CLI 写入示例：

```bat
pangdun.cmd collab update 36 --status 运输中 --apply --reason "物流已确认揽收"
```

## 9. 本次交接时的验证状态

- `npm run build` 通过。
- 已在本地浏览器验证合作执行三个视图控制栏位置固定。
- 已验证完整状态轨道与顶部下拉筛选同步。
- 已验证当前“今天/逾期/未来 7 天均为 0、全部待办为 3”时，首页自动打开全部待办。
- 已验证用户手动选择“今天 · 0”后，切换到其他视图再返回仍保留该选择。
- 浏览器验证期间未发现 console error/warning。
- 已建立前端 Vitest + Testing Library 测试，覆盖智能待办落点、状态轨道同步和详情内容登记后刷新。
- 已建立后端 pytest 临时 SQLite 集成测试，覆盖 OA/PI/物流保存、状态留痕、归档隐藏与恢复。
- 测试数据库通过 `PANGDUN_DATABASE_URL` 注入，不读取或改写正式业务数据库。
